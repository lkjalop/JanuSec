# Pragmatic Security Threat Sifting Platform - Core Dependencies
# Author: Security Engineering Team
# Version: 1.0.0

# Core async framework
asyncio-mqtt>=0.13.0
aiohttp>=3.9.0
aiofiles>=23.2.0

# Core data processing (lightweight subset)
numpy>=1.24.0
pandas>=2.0.0

# Efficient data structures
pybloom-live>=4.0.0
mmh3>=4.0.0

# Configuration and serialization
PyYAML>=6.0
# NOTE: Codebase mixes v1/v2 styles in places; pin to <3.0. Adjust if strictly v2.
pydantic>=2.0.0,<3.0

# Database connections
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
aioredis>=2.0.0
aiosqlite>=0.19.0
redis>=4.6.0
syslog-rfc5424-parser>=0.1.7

# Monitoring and metrics
prometheus-client>=0.19.0
# collectors
prometheus-client>=0.16.0
syslog-rfc5424-parser>=0.1.7
structlog>=23.2.0
opentelemetry-api>=1.16.0
opentelemetry-sdk>=1.16.0
opentelemetry-instrumentation-aiohttp-client>=0.36b0
aiohttp>=3.8.0
lark>=1.1.0

# Scheduler for background jobs
apscheduler>=3.10.1

# OpenTelemetry (optional; enable with envs)
opentelemetry-sdk>=1.26.0
opentelemetry-exporter-otlp>=1.26.0
opentelemetry-instrumentation-fastapi>=0.47b0
opentelemetry-instrumentation-asgi>=0.47b0
# Optional breadth
opentelemetry-instrumentation-httpx>=0.47b0
opentelemetry-instrumentation-asyncpg>=0.47b0

# Security and cryptography
cryptography>=41.0.0
# hashlib is part of stdlib; removed hashlib-compat which is unnecessary

# HTTP client with retries
httpx>=0.25.0
tenacity>=8.2.0

# Testing and development
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0

###############################################
# Optional / ML / Analysis moved to requirements-ml.txt
# regex, orjson, cachetools retained only if genuinely needed downstream.
###############################################
regex>=2023.10.0
orjson>=3.9.0
cachetools>=5.3.0
datasketch>=1.6.0

# API framework & server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
openpyxl>=3.1.0
requests>=2.31.0
python-multipart>=0.0.9
# Async file upload ingest pipeline
duckdb>=0.10.0
ijson>=3.2.0
# Secret backends
hvac>=1.2.1
# Optional document processing (install if generating or parsing PDFs)
reportlab>=4.0.0
pdfplumber>=0.11.0

# Coverage reporting
pytest-cov>=4.1.0

# Integrations (comment in as needed)
# slack_sdk>=3.27.0        # Slack notifications
# twilio>=9.0.0            # WhatsApp/SMS alerts
# msal>=1.28.0             # Advanced Teams auth flows

# Windows note: uvloop not installed; if on Linux add:
# uvloop>=0.19.0 ; platform_system != 'Windows'

# Static analysis
# Use latest available pefile release compatible with PyPI
pefile>=2024.8.26
