apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: janusec-cert
  namespace: default
spec:
  secretName: janusec-tls
  issuerRef:
    name: letsencrypt-http01
    kind: ClusterIssuer
  dnsNames:
    - janusec.example.com
