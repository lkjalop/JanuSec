# Performance Benchmarks

This folder contains guidance and scripts to reproduce performance claims using synthetic events. The repository includes a simple event generator; use k6 or Locust for load testing against a deployed demo endpoint.

Example (k6):

```bash
# install k6
# run a scenario that posts synthetic events to your local demo API
```
