input {
  file { path => "C:/logs/*.json" start_position => "beginning" sincedb_path => "nul" } 
}
filter {
  json { source => "message" }
}
output {
  http {
    url => "http://localhost:8080/api/v1/events"
    http_method => "post"
    format => "json"
    headers => { "x-api-key" => "devkey123" }
  }
}
