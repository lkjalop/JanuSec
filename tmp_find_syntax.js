const fs=require('fs'); const vm=require('vm'); const s=fs.readFileSync('frontend/static/js/csv_analyzer.js','utf8'); let lo=0, hi=s.length; let lastGood=0; while(lo<hi){ let mid=Math.floor((lo+hi)/2); try{ new vm.Script(s.slice(0,mid)); lastGood=mid; lo=mid+1; }catch(e){ hi=mid; } } console.log('lastGood index', lastGood); const contextLines = s.slice(Math.max(0,lastGood-200), lastGood+200); console.log('context around failure:\n', contextLines);