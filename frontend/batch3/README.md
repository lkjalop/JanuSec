Batch 3 frontend modernization — scaffolding

Purpose:
- Continue frontend resilience and developer ergonomics after Batch 2.
- Focus areas: offline-friendly enhancements, performance (lazy loading + code-splitting), telemetry, accessibility, and feature gated rollout.

Structure:
- `batch3/BATCH3.md` — plan and milestones
- `batch3/tasks.json` — actionable tasks tracked

Runbook:
- Review Batch 2 artifacts (`fetch_wrapper.js`, `stream_client.js`, SW changes).
- Implement prioritized items in small increments and run Playwright smoke tests after each major change.
