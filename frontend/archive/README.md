# Archived frontends

These pages are retained for archaeology and regression comparison only. The
supported UI is `frontend/static/janusec-platform-complete-LIVE.html`, backed by
the server-owned CaseEvidenceViewModel.

Archived on 2026-08-18:

- `assessment.html`: independent prototype; embedded API keys in URLs.
- `investigation.html`: independent prototype; embedded API keys in URLs.

Do not restore either page as an application entry point. Promote any useful
interaction into the canonical case workspace and its server contract.
