# Archived frontend

Moved out of the active build as part of the describe-only / GRC pivot.

- react/ — legacy React SPA; was never referenced by any served HTML (the /react and /assets mounts are existence-guarded, so removal is safe).
